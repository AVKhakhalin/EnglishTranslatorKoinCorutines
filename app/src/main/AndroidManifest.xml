<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.geekbrains.popular.libraries.englishtranslatorkoincorutines">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Разрешения на запись информации на диск телефона -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Разрешения на запись информации на диск телефона -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <!-- This attribute is "false" by default on apps targeting Android Q. -->
    <!-- Включена поддержка больших файлов (largeHeap) -->
    <application
        android:requestLegacyExternalStorage="true"
        android:largeHeap="true"

        android:name=".application.TranslatorApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DayTheme">
        <activity
            android:name=".view.main.MainActivity"
            android:theme="@style/SplashTheme"
            android:exported="true"> <!--Нужно для устранения ошибки: Manifest merger failed : Apps targeting Android 12 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>